# Dependencies
node_modules/
# package-lock.json - Keep for CI/CD caching and reproducible builds
# yarn.lock - Keep if using Yarn
# pnpm-lock.yaml - Keep if using pnpm

# Build outputs
dist/
dist-ssr/
build/
*.local

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
*.env

# Firebase secrets and credentials
firebase-service-account.json
*-service-account.json
service-account*.json
firebase-adminsdk-*.json
# .firebaserc - Allow (contains only project ID mapping, no secrets)
# .firebase/ - Ignore (contains local cache)
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Firebase tokens and keys
.firebase-token
firebase-token.txt
FIREBASE_TOKEN
*.pem
*.key

# Firebase configuration
# Note: firebase.json is tracked (no secrets, just configuration)
# firebase.json.example is also tracked
!firebase.json.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
Thumbs.db
.DS_Store

# Database
*.db
*.sqlite
*.sqlite3

# Temporary files
*.tmp
*.temp
.cache/

# Secrets and credentials
secrets/
*.secret
credentials.json
config.json
!firebase.json.example
!*.example

# CI/CD secrets (if accidentally committed)
.github/secrets/
*.token
*.key
