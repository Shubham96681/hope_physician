# Dependencies
node_modules/
# package-lock.json - Keep for CI/CD caching and reproducible builds

# Database
prisma/*.db
prisma/*.db-journal
prisma/migrations/

# Environment variables
.env
.env.local
.env.*.local
*.env

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build
dist/
build/

# Firebase secrets and credentials
firebase-service-account.json
*-service-account.json
service-account*.json
firebase-adminsdk-*.json
.firebaserc
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Firebase tokens and keys
.firebase-token
firebase-token.txt
FIREBASE_TOKEN
*.pem
*.key

# Firebase configuration (if contains secrets)
firebase.json
!firebase.json.example

# Secrets and credentials
secrets/
*.secret
credentials.json
config.json
!*.example

# CI/CD secrets (if accidentally committed)
*.token
*.key

